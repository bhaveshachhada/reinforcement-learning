[project]
name = "environments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bhavesh Achhada", email = "bhavesh@agricarbon.co.uk" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
environments = "environments:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
